import { RemoveChartDirective } from './remove-chart.directive';

describe('RemoveChartDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveChartDirective();
    expect(directive).toBeTruthy();
  });
});
